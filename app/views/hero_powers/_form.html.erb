<%= form_with(model: hero_power) do |form| %>
  <% if hero_power.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hero_power.errors.count, "error") %> prohibited this hero_power from being saved:</h2>

      <ul>
        <% hero_power.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :strength, style: "display: block" %>
    <%= form.text_field :strength %>
  </div>

  <div>
    <%= form.label :hero_id, style: "display: block" %>
    <%= form.number_field :hero_id %>
  </div>

  <div>
    <%= form.label :power_id, style: "display: block" %>
    <%= form.number_field :power_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
